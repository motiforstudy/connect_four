



def in_bounds(board: list[list[str]], row: int, col: int) -> bool:

    return True


def has_winner_from(board: list[list[str]], row: int, col: int) -> bool:

    return True


def has_winner(board: list[list[str]]) -> bool:

    return True


def is_full(board: list[list[str]]) -> bool:

    return True


def game_over(board: list[list[str]]) -> tuple[bool, str | None]:

    return tuple (True, "")